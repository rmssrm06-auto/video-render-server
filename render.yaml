services:
  - type: web
    name: video-render-server
    env: node
    # This command installs FFmpeg on the server before starting your app.
    # FFmpeg is what actually combines image + audio into video.
    buildCommand: apt-get update && apt-get install -y ffmpeg && npm install
    startCommand: npm start
    plan: free
