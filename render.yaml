services:
  - type: web
    name: video-render-server
    # Instead of a plain Node environment, we use a Docker image
    # that already has FFmpeg pre-installed. This bypasses the
    # read-only filesystem restriction entirely.
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
